<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="flex w-full justify-end">
        <a
          class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block"
          href="/logout"
          >Logout</a
        >
      </div>
      <h3 class="text-3xl">Hello,</h3>
      <h5 class="mb-5">Edit your post.</h5>
      <form action="/edit/<%= post._id %>" method="post">
        <textarea
          class="resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none block w-1/3"
          name="content"
          id="post"
          placeholder="what's in your mind"
        ><%= post.content %></textarea>
        <input
          class="px-3 py-2 bg-yellow-500 block rounded-md mt-2 w-40"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
